import { ViewpasswordPipe } from './viewpassword.pipe';

describe('ViewpasswordPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewpasswordPipe();
    expect(pipe).toBeTruthy();
  });
});
